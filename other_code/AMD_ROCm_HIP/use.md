These folders have HIP code that is relevant to basal, for GPU acceleration.

Compile the code, or run the executable to use the program.

Look at the code before running it to know how to interact with it - pass arguments in the command line or type numbers when prompted.