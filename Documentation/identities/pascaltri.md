# Identity of any binomial expansion

For any binomial expansion of 2 numbers `q` and `i` with the exponent `r`:
```
        z=0
  < q T  এ 1  C(r, z)  T i >
         r
```
The character '<' is still supposed to have a line above it
