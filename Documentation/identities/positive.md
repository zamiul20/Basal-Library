# Identity of Most Positive Numbers

For any positive numbers `q` and `i`, where `q` is not equal to 1:
$$< q \; স \;(\log_{q}(i))\;|\; 1\; 0 = i$$
The characters '<' and '|' are still supposed to have a line above them
